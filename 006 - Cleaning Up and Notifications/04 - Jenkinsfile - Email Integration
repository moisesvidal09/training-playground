pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                sh "echo 'Hello, World !'"
            }
        }
    }
    post {
        always {
            emailext body: 'Jenkins E-mail', recipientProviders: [[$class: 'DevelopersRecipientProvider'], [$class: 'RequesterRecipientProvider']], subject: 'moises.vidal@celfocus.com'
        }
    }
}
